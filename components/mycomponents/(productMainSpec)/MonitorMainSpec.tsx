import { Product } from "@/payload-types";

interface MonitorMainSpecProps {
  product: Product;
}

const MonitorMainSpec = ({ product }: MonitorMainSpecProps) => {
  return <div>MonitorMainSpec</div>;
};
export default MonitorMainSpec;
